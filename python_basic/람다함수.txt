람다 함수 ( 익명 함수 )
파이썬에서는 람다함수를 통해 이름 없는 함수를 만들 수 있다. 
이러한 람다함수의 장점은 코드의 간결함과 메모리 절약이다.
def 함수이름(매개변수) : 
	return 결과

-> lambda 매개 변수 : 결과 
(lambda 인자리스트: 표현식)
(lambda x : x+1)(3) 
>> 4

단점 다시 사용할 수 없다. 
 filter(), map(), reduce() 같은 기능과 함께 사용되기도 한다. 
